datasource db {
  provider = "sqlite"
  url      = "file:./main.db"
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id   Int    @id @default(autoincrement())
  name String @unique
  salt Bytes
  hash Bytes
}
